- name: check the background
  apt:
    update_cache: true
    cache_valid_time: 3600

- name: Gather OS Information
  setup:
    gather_subset:
      - "os_family"
- name: print OS Family
  debug:
    msg: "The Under OS is: {{ansible_os_family}}"

